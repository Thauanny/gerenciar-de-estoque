<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrimeiroApp">
        <activity
            android:name=".presenter.view.CadastroLoginActivity"
            android:label="@string/titleCadastroLogin"
            android:parentActivityName=".presenter.view.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.view.UpdateActivity"
            android:exported="false"
            android:label="@string/titleUpdateScreen"
            android:parentActivityName=".presenter.view.MenuActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presenter.view.DeletarActivity"
            android:exported="false"
            android:label="@string/buttonDeletar"
            android:parentActivityName=".presenter.view.MenuActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presenter.view.ProdutosActivity"
            android:exported="false"
            android:label="@string/appBarTitleProdutos"
            android:parentActivityName=".presenter.view.MenuActivity" />
        <activity
            android:name=".presenter.view.CadastrarProdutoActivity"
            android:exported="false"
            android:label="@string/appBarTitleCadastro"
            android:parentActivityName=".presenter.view.MenuActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presenter.view.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presenter.view.MenuActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.view.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>